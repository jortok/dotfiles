# =========================================================
# Contenido para: ~/.config/zsh/.zshrc
# Se carga para terminales INTERACTIVAS.
# =========================================================
# Carga alias, funciones y la configuración interactiva de Zsh.
[ -f "$HOME/.config/shell/zsh_specific.zsh" ] && source "$HOME/.config/shell/zsh_specific.zsh"
