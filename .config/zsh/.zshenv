# =========================================================
# Contenido para: ~/.config/zsh/.zshenv
# Se carga SIEMPRE. Perfecto para variables de entorno.
# =========================================================
# Carga las variables de entorno compartidas.
[ -f "$HOME/.config/shell/shared_env.sh" ] && source "$HOME/.config/shell/shared_env.sh"
